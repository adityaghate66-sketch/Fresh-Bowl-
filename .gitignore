node_modules
dist
.DS_Store
*.local

# Secrets — NEVER commit these
.env
*.pem
*.keystore
*.jks

# Internal tool state
.freebuff/

# Android build outputs (added when we do the APK phase)
*.apk
android/.gradle/
android/app/build/
android/build/
android/local.properties
